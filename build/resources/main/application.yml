# server配置
server:
  port: 8088
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /taskdemo

# spring配置
spring:
  # 连接池
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.98.157.120:3306/gisadmin.v1.2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2b8&useSSL=true&tinyInt1isBit=false
    username: root
    password: idontcare

# MyBatisPlus 配置
mybatis-plus:
  # 映射文件所在路径
  mapper-locations: classpath:mapper/*.xml
  typeAliasesPackage: com.zhuzs.taskdemo.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 日志记录
logging:
  config: classpath:logback.xml
